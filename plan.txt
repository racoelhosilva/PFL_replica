% State -> state(Board, CurrentPlayer, WhiteKingEaten, BlackKingEaten)
% Add more variables when more game modes are implemented

% GameConfig -> gameConfig(GameMode, Player1Info, Player2Info)
% PlayerInfo -> playerInfo(Name, Difficulty)
